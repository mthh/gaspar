/**
* The Object used later as a container for the various features handled in the
* application. Each value is expected to be an Array of GeoJSON Feature
* (with non null geometry in EPSG:4326).
*/
const DB = {};
export default DB;
